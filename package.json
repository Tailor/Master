{
  "name": "master",
  "version": "0.0.7",
  "description": "A framework to build MVC applications in node",
  "main": "./cli.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Batista",
  "license": "ISC",
  "dependencies": {
    "commander": "^3.0.1",
    "fs-extra": "^8.1.0"
  },
  "bin": {
    "master": "./cli.js"
  }
}
